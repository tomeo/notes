# Getting the inverse of the match with Grep

The following will match anything that doesn't include `foo`:
```
cat file | grep -v foo
```

## Documentation
* https://www.gnu.org/savannah-checkouts/gnu/grep/manual/grep.html